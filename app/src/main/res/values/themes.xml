<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base theme used only for the splash screen, then hands off to Compose Material3 -->
    <style name="Theme.LetsFootball" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.LetsFootball.Main</item>
    </style>

    <style name="Theme.LetsFootball.Main" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
    </style>

</resources>
